NEXT_PUBLIC_API_URL=http://localhost:5002
NEXT_PUBLIC_SITE_URL=https://dgdelightfound.org
# Optional: Google Search Console verification meta content value
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
# Shared with backend — used by POST /api/revalidate after admin CMS updates
REVALIDATE_SECRET=change_me_revalidate_secret
# Optional: force no-store CMS fetches in production (default uses 60s ISR)
# CMS_CACHE_MODE=no-store
